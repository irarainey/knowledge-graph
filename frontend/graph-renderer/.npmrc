# Pull all npm packages through the Microsoft CFS-protected feed instead of the
# public registry (registry.npmjs.org), which is blocked on company-managed
# devices. pnpm reads this file automatically. Do not point this at the public
# registry — see the security rollout for the NPM Minimum Release control.
registry=https://packagefeedproxy.microsoft.io/npm/
